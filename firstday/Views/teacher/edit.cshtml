@model firstday.Models.teacher
@{
    ViewBag.Title = "edit";
}
<link rel="stylesheet" href="~/Content/index.css">
<h2>Edit details</h2>
@using (Html.BeginForm("updateData", "teacher")) //teacher is controller name
{
    <input type="hidden" name="id" value="@Model.id" />
    <label class="col-md-2">Name</label>
    <div class="col-md-4">
        <input type="text" name="name" class="form-control" value="@Model.name" required>
    </div>
    <label class="col-md-2">Salary</label>
    <div class="col-md-4">
        <input type="text" name="salary" class="form-control" value="@Model.salary" required>
    </div>
    <button class="btn">Edit</button>
}
